# Routing Engine (Whitefield → Majestic)

This project demonstrates:
- Geocoding using OpenStreetMap Nominatim
- Routing using OSRM
- Extracting distance, duration, and steps

## How to run

Install dependencies:
pip install -r requirements.txt

Run the script:
python route_whitefield_to_majestic.py
